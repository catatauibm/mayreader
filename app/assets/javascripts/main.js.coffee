# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

# $(function() {
  # $(".suspension-points").each(function() {
    # var val = $(this).html();
    # if (val.length > 10) {
    # var new_val = val.substr(0,10).concat("...");
    # $(this).html(new_val);
  # }
  # });
# });

# 
# $
  # printSuspensionPoints: (qteChr) ->
    # val = $(this).html()
# 
    # @each ()
      # if val.length > 10
        # new_val = val.substr(0,10).concat("...")
        # $(this).html(new_val)
#   
# 
# 
# $('.suspension-points').printSuspensionPoints(10)

# (function($) {
  # $.fn.printSuspensionPoints = function(qteChr) {
        # alert(this.text());
      # // Usually iterate over the items and return for chainability:
      # return this.each(function() { 
           # // do something to each item matching the selector
          # alert($(this).text());
      # });
   # }
# }(jQuery)); 
# // (function($) {
  # // $.fn.printSuspensionPoints = function(qteChr) {
    # // return this.each(function() {
      # // alert(qteChr);
      # // // var val = $(this).html();
      # // // alert(val);
      # // // if (val.length > qteChr) {
        # // // var new_val = val.substr(0, qteChr).concat("...");
        # // // $(this).html(new_val);
      # // // }
    # // });
# //    
    # // // this.filter( "a" ).each(function() {
      # // // var link = $( this );
      # // // link.append( " (" + link.attr( "href" ) + ")" );
    # // // });
    # // // return this;
  # // };
# // }(jQuery));
# // 
       # $('.suspension-points').printSuspensionPoints(10);

